{
  "devDependencies": {
    "husky": "^7.0.4",
    "lint-staged": "^12.3.3",
    "prettier": "^2.5.1"
  },
  "scripts": {
    "prepare": "husky install",
    "format": "prettier --write \"**/*.{json,md,yaml}\""
  },
  "lint-staged": {
    "*.{json,md,yaml}": "prettier --write"
  }
}
